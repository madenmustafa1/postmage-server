package com.postmage.extensions

/*
import com.google.gson.Gson
import io.javalin.http.Context
import com.postmage.model.token.TokenDataModel
import util.TokenUtil

fun Context.authToDataClass(): TokenDataModel? {
    val gson = Gson()
    return gson.fromJson(TokenUtil.decodeToken(this.header("Authorization")!!), TokenDataModel::class.java)
}
 */